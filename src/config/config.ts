/**
 * config.ts
 * 
 * Author: Mert Özdemir <mertozdemircontact@icloud.com>
 */

import dotenv from "dotenv";

const initServerConfigurations = (): void => {
      dotenv.config();
};

export default initServerConfigurations;
/**
 * jwt_interface.ts
 * JWT decoded token interface
 * 
 * Author: Mert Özdemir <mertozdemircontact@icloud.com>
 */

export interface DecodedTokenInterface{
    id: string, // Adjust the type based on your actual token structure
    first_name: string // Adjust the type based on your actual token structure
};